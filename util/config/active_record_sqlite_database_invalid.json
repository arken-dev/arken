{
  "development" : {
    "adapter"  : "ActiveRecord.PostgresAdapter",
    "host"     : "localhost",
    "database" : "mydatabase",
    "user"     : "postgres",
    "password" : "mypassword"
  },
  "production" : {
    "adapter"  : "ActiveRecord.PostgresAdapter",
    "host"     : "localhost",
    "database" : "mydatabase",
    "user"     : "postgres",
    "password" : "mypassword"
  },
  "test" : {
    "adapter"  : "ActiveRecord.SqliteAdapter",
    "database" : "/database-not-exists/database.db"
  }
}
